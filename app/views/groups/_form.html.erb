<%= form_for @group do |f| %>
<% if @group.errors.any? %>
<ul>
  <% @group.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
</ul>
<% end %>
标题
<%= f.txet_field :title %>
<br>
叙述
<br>
<%= f.text_area :descriptioin %>
<br>

<%= f.fubmit "Submit", :disable_with => 'Submiting...' %>
<% end %>
